name: DevSecOps Pipeline
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

# TODO#0: Declare workflow permissions.
permissions:
  contents: read
  security-events: write  # TODO#0a: required to upload SARIF to GitHub Security tab
  issues: write            # TODO#0b: so Gitleaks/Sonar can comment on PRs + issues
  pull-requests: write     # TODO#0b: so Gitleaks/Sonar can comment on PRs + issues
  id-token: write          # TODO#0c: for OIDC token (e.g. Snyk auth)
  actions: read            # needed for some actions to list workflow runs

jobs:
    # GATE#0: Secret scanning (TruffleHog + Gitleaks)
    secret-scanning:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout code
              uses: actions/checkout@v4
              with:
                # TODO#1: Secret scanners need full git history to find secrets in past commits
                fetch-depth: 0

            - name: Run TruffleHog
              uses: trufflesecurity/trufflehog@main
              continue-on-error: true
              with:
                path: ./
                # TODO#2: set base + HEAD refs so TruffleHog scans the commit diff
                base: ${{ github.event.repository.default_branch }}
                head: HEAD
                # TODO#3: report ONLY VERIFIED secrets (reduce noise)
                extra_args: --only-verified

            # TODO#4: Gitleaks step
            - name: Run Gitleaks
              uses: gitleaks/gitleaks-action@v2
              continue-on-error: true
              env:
                GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
                GITLEAKS_ENABLE_UPLOAD_ARTIFACT: true
                GITLEAKS_ENABLE_SUMMARY: true

    # GATE#1a: SAST via SonarQube
    sast-scan:
        runs-on: ubuntu-latest
        needs: secret-scanning
        if: ${{ !cancelled() }}
        steps:
            - name: Checkout code
              uses: actions/checkout@v4
              with:
                # TODO#5: Sonar needs full history for blame/new-code detection
                fetch-depth: 0

            # TODO#6: SonarQube scan step
            - name: SonarQube Scan
              uses: SonarSource/sonarqube-scan-action@v7.1.0
              continue-on-error: true
              env:
                  SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}

    # GATE#1b: SAST via CodeQL (results in GitHub Security tab)
    codeql-scan:
        runs-on: ubuntu-latest
        needs: secret-scanning
        if: ${{ !cancelled() }}
        strategy:
            matrix:
                # TODO#7: Next.js uses JavaScript for React, JavaScript for API routes
                language: [ 'javascript' ]
        steps:
            - name: Checkout code
              uses: actions/checkout@v4

            - name: Initialize CodeQL
              uses: github/codeql-action/init@v3
              with:
                languages: ${{ matrix.language }}
                # TODO#8: enable both security-extended and security-and-quality suites
                queries: security-extended,security-and-quality

            # TODO#9: CodeQL analyze step
            - name: Perform CodeQL Analysis
              uses: github/codeql-action/analyze@v3
              with:
                category: "/language:${{ matrix.language }}"

    # GATE#2: Dependency scanning via OWASP Dependency-Check
    dependency-scanning:
        runs-on: ubuntu-latest
        needs: [sast-scan, secret-scanning]
        if: ${{ !cancelled() }}
        steps:
            - name: Checkout code
              uses: actions/checkout@v4

            # TODO#10: set up Node 20
            - name: Set up Node.js
              uses: actions/setup-node@v4
              with:
                node-version: '20'

            # TODO#11: install npm deps so Dependency-Check can resolve full tree
            - name: Install dependencies
              run: npm install

            - name: Depcheck
              uses: dependency-check/Dependency-Check_Action@main
              continue-on-error: true
              id: Depcheck
              with:
                project: 'DontEverPushThisCodeV2'
                path: '.'
                # TODO#12: SARIF format integrates with GitHub Security tab
                format: 'SARIF'
                out: 'reports'
                # TODO#13: fail on CVSS >= 7 (HIGH severity threshold)
                args: >
                    --failOnCVSS 7
                    --enableRetired

            # TODO#14: upload Depcheck SARIF to GitHub Security tab
            - name: Upload Depcheck SARIF to Security tab
              if: always()
              uses: github/codeql-action/upload-sarif@v3
              continue-on-error: true
              with:
                  sarif_file: reports/dependency-check-report.sarif
                  category: dependency-check

            # TODO#15: upload raw Depcheck report folder as artifact
            - name: Upload Depcheck HTML report as artifact
              if: always()
              uses: actions/upload-artifact@v4
              with:
                name: depcheck-report
                path: reports/

    # GATE#3: Container/Image scanning via Trivy
    image-scanning:
        runs-on: ubuntu-latest
        needs: [dependency-scanning]
        if: ${{ !cancelled() }}
        steps:
            - name: Checkout code
              uses: actions/checkout@v4

            # TODO#16: build the Docker image
            - name: Build Docker image
              run: docker build -t my-app:latest .

            - name: Trivy Scan
              uses: aquasecurity/trivy-action@master
              continue-on-error: true
              with:
                image-ref: 'my-app:latest'
                # TODO#17: SARIF format for Security tab integration
                format: 'sarif'
                output: 'trivy-results.sarif'
                # TODO#18: report HIGH and CRITICAL severities
                severity: 'HIGH,CRITICAL'
                # TODO#19: include CVEs even without a fix available
                ignore-unfixed: false

            # TODO#20: upload Trivy SARIF to Security tab
            - name: Upload Trivy SARIF to Security tab
              if: always()
              uses: github/codeql-action/upload-sarif@v3
              continue-on-error: true
              with:
                sarif_file: trivy-results.sarif
                category: trivy

            # TODO#21: upload trivy-results.sarif as workflow artifact
            - name: Upload Trivy SARIF as artifact
              if: always()
              uses: actions/upload-artifact@v4
              with:
                name: trivy-results
                path: trivy-results.sarif
